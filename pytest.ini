# pytest.ini
[pytest]
minversion = 7.0
addopts = -ra --strict-markers --tb=short --asyncio-mode=auto
testpaths = tests
log_cli = true
log_level = INFO
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
